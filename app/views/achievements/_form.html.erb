<%= simple_form_for achievement do |f| %>
  <%= f.input :individual_id, as: :hidden, :input_html => { :value => @individual.id } %>
  <%= f.input :description, label: false %>
  <%= f.submit "#{achievement.new_record? ? "Create" : "Update"}"%>
<% end %>
